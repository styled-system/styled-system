import RebassMDX from '@rebass/mdx'
import { Link } from 'gatsby'

export default RebassMDX.props({
  wrap: true,
  theme: {
    styles: {
      wrapper: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
        height: 64,
      },
      h2: {
        fontSize: [2, 2],
        a: {
          display: 'block',
          p: 2,
          color: 'inherit',
          '&:hover': {
            textDecoration: 'none',
          }
        }
      },
      ul: {
        listStyle: 'none',
        display: 'flex',
      },
      a: {
        display: 'block',
        fontWeight: 'bold',
        color: 'inherit',
        textDecoration: 'none',
        p: 3,
      },
    }
  }
})

## <Link to='/'>Styled System</Link>

<button
  style={{
    margin: 16,
  }}
  onClick={e => {
    props.setOpen(!props.open)
  }}>
  Menu
</button>
